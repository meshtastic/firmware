# Local-only sandbox broker. Two listeners: native MQTT for meshtasticd / bridge,
# WebSockets for the test UI in the browser. allow_anonymous is OK on localhost
# only — never copy this config into anything reachable from a network.

persistence false
allow_anonymous true

listener 1883
protocol mqtt

listener 9001
protocol websockets
