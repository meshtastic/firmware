# FAULT: an LR20x0 with no rfswitch_table, so setRfSwitchTable() is never called. Expect a
# WARNING from the merged-configuration checks. DIO5 is left as the interrupt to show the
# collision check stays silent when there is no table.
Lora:
  Module: lr2021
  spidev: spidev0.0
  CS: 21
  IRQ: 16
  Busy: 20
  Reset: 18
  IRQ_DIO_NUM: 5
